# Databricks CI/CD Sync

This repository implements a CI/CD pipeline to automate code synchronization between GitHub and Databricks Repos. Utilizing **GitHub Actions** and the **Databricks CLI**, it triggers updates to the target Databricks workspace whenever changes are pushed to the main branch, ensuring seamless deployment and version control.
